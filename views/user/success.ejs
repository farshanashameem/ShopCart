<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head', { title: 'ShopCart - Order Success' }) %>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    .success-container {
      text-align: center;
      margin: 5% auto;
      padding: 20px;
      max-width: 600px;
    }

    .success-icon {
      font-size: 80px;
      color: #28a745; /* green */
      margin-bottom: 20px;
    }

    .success-message {
      font-size: 1.5rem;
      font-weight: bold;
      margin-bottom: 30px;
    }

    .btn-group {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 15px;
    }

    .btn-custom {
      border: 2px solid #000;
      padding: 10px 25px;
      border-radius: 25px;
      background: transparent;
      transition: all 0.3s ease;
    }

    .btn-custom:hover {
      background: #000;
      color: #fff;
    }

    /* Responsive */
    @media (max-width: 576px) {
      .success-icon {
        font-size: 60px;
      }
      .success-message {
        font-size: 1.2rem;
      }
      .btn-custom {
        flex: 1;
        text-align: center;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="success-container bg-light">
    <div class="success-icon">✔</div>
    <div class="success-message">THANK YOU FOR ORDERING</div>
   
    <div class="btn-group">
      <a href="/orders" class="btn-custom text-decoration-none text-dark">VIEW ORDERS</a>
      <a href="/products" class="btn-custom text-decoration-none text-dark">CONTINUE SHOPPING</a>
       <a href="/invoice/<%= orderId %>"  type="button" class="btn btn-dark mt-3">Invoice</a>
    </div>
  </div>

  <%- include('../partials/footer') %>
</body>
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</html>
