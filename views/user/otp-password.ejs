<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title: 'otp for Password' }) %>
  <body class=" d-flex justify-content-center align-items-center vh-100" >

    <div class="container p-4 shadow rounded-4 overflow-hidden " style="max-width: 900px;background-color: rgb(244, 236, 227) !important;">
      <div class="row g-0">

        <!-- left Section (Image) -->
        <div class="col-md-6 d-none d-md-block">
          <img src="/images/otp-password.png" alt="password Visual" class="img-fluid h-100 w-100 object-fit-cover" style="object-fit: cover;" />
        </div>

        <!-- Right Section OTP -->
        <div class="col-md-6  p-5 d-flex flex-column justify-content-center">
          <h2><b>Confirm with OTP</b></h2>
          <p><b>Please Check your mail for OTP</b></p>
         
            <% if(error) {%>
                 <p class="text-danger"> <%= error %></p>
            <%}%>
          

          <form action="/verifyOtpForPassword" method="POST">
            <div class="d-flex justify-content-center gap-2 mb-3">
              <input type="hidden" name="email" value="<%= email %>">
            <input type="text" name="otp[]" maxlength="1" class="form-control text-center otp-input" required>
            <input type="text" name="otp[]" maxlength="1" class="form-control text-center otp-input" required>
            <input type="text" name="otp[]" maxlength="1" class="form-control text-center otp-input" required>
            <input type="text" name="otp[]" maxlength="1" class="form-control text-center otp-input" required>
            </div>

            <button type="submit" class="btn btn-dark w-100">Verify OTP</button>
         </form>
            <p class="mt-3 text-muted" id="resend-container">Resend OTP in <span class="text-start text-muted" id="timer"><%= typeof remainingTime !== 'undefined' ? remainingTime : 60 %></span>seconds.
            </p>

            <a href="/resend-otp" id="resend-link" class="d-none text-primary text-decoration-none">Resend OTP</a>
            
          <p class="mt-4 text-center text-muted">
          <a href="#" onclick="event.preventDefault(); window.location.href='/login';">Go back to login page</a>
          </p>
        </div>

        
      </div>
    </div>

    <!-- Include your external script -->
    <script src="/js/otp-timer.js"></script>
  </body>
</html>
