<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title: 'Edit colour' }) %>
  <body >
    <div class="container-fluid ">
      <div class="row min-vh-100" >
         <%- include('../partials/adminDashboard') %>

        <!-- Main Content -->
         <div class="col-md-8 d-flex flex-column m-4 text-center" >
            <h1 class="text-center">Edit Colour</h1>
            <% if(error) { %>
                <p class="text-danger"><b><%= error%></b></p>
            <%} %>
            <div class="d-flex justify-content-center mt-5">
                <form action="/admin/editColour" method="post" class="d-flex flex-column p-4 gap-4 col-md-6">
                    <input type="hidden" name="id" value="<%= colour._id %>">
                    <label for="name"></label>
                    <input type="text" name="name" placeholder="Name" class="form-control" value="<%= oldInput?.name || colour.name %>"/>
                    
                    <label for="hexcode"></label>
                    <input type="text" name="hexCode" placeholder="Hex code" class="form-control" value="<%= oldInput?.hexCode || colour.hexCode %>" />
                    
                    <button class="btn btn-dark" type="submit">Edit</button>
                </form>
            </div>


         </div>
        
    </div>
             <footer class="text-center py-4 small text-muted border-top mt-4" style="font-weight: 700;">
            © 2025 | ShopCart | All Rights Reserved. | We Give You Style.
            </footer>
    </div>

   

    <!-- Bootstrap & Icons -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
