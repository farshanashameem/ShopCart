 <% if (typeof successMessage !== 'undefined' && successMessage) { %>
    <script>
      Swal.fire({
        icon: 'success',
        title: 'Success',
        text: "<%= successMessage %>",
        timer: 2000,
        showConfirmButton: false
      });
    </script>
    <% } %>

    